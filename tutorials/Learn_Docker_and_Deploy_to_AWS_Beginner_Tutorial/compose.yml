services:
  app:
    build: .
    image: test_container
    container_name: test_container_app
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - shared_volume:/app/shared_folder

volumes:
  shared_volume:
